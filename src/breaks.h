#ifndef BREAKS_H_
#define BREAKS_H_

int periodicMatching(            const char*     text, 
                                 const char*     pattern,
                                       int       k,
                                       int       n,
                                       int       m,
                                       int*      matches                      );

#endif

int test_breaks();
